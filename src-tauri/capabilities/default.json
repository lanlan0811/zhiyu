{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: core + shell (daemon lifecycle is driven from the Rust shell; the shell plugin is initialized for process/URL helpers).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
